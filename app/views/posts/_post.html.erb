<div id="<%= dom_id post %>">
  <h2>
    <%= post.title %>
  </h2>
  <h5> POsted by : <%= link_to post.user.name, user_path(post.user), style: "text-decoration: none; color: blue;"%></h5>
  <h6><%= pluralize(post.views, "view") %></h6>
  <div>
    <%= post.body %>
  </div>
</div>
